# نظام الترجمة باستخدام Google Translate

## 📋 نظرة عامة

تم استبدال نظام الترجمات اليدوية بنظام **Google Translate Web Element** مع واجهة مستخدم مخصصة (Custom UI).

## 🔧 كيفية العمل

### 1. المكونات الرئيسية

#### `google-translate-controller.js`
- **الدور**: التحكم ببرمجة في Google Translate
- **الميزات**:
  - إنشاء واجهة مخصصة لاختيار اللغة
  - إخفاء واجهة Google الافتراضية تماماً
  - إدارة الرسائل الثابتة (Static Messages)
  - حفظ تفضيلات اللغة في localStorage

#### `index.html`
- يتضمن Google Translate Script من CDN
- عنصر مخفي (`#google_translate_element`) للتحكم الداخلي
- أزرار اللغات المخصصة مع `data-google-lang` attribute

#### `app.js`
- تحديث رسائل النظام عند تغيير اللغة
- دالة `updateUIMessages()` لتحديث النصوص الثابتة

### 2. الرسائل الثابتة (System Messages)

الرسائل الثابتة مثل "ادفع" و "السلة" مُعرّفة في `google-translate-controller.js`:

```javascript
const systemMessages = {
    ar: {
        cartLabel: "ادفع",
        cartTitle: "السلة",
        // ... المزيد
    },
    en: {
        cartLabel: "Pay",
        cartTitle: "Cart",
        // ... المزيد
    }
    // ... اللغات الأخرى
};
```

### 3. تدفق العمل عند اختيار لغة

```
المستخدم ينقر على زر اللغة
    ↓
selectGoogleLanguage(googleLang) - تحديث القائمة المخفية
    ↓
Google Translate يترجم المحتوى
    ↓
updateSystemMessages(lang) - تحديث الرسائل الثابتة
    ↓
hideGoogleTranslateBar() - إخفاء الشريط
```

### 4. إخفاء واجهة Google Translate

تم إخفاء شريط جوجل بثلاث طرق:

1. **CSS** (في styles.css):
```css
.skiptranslate {
    display: none !important;
}
.goog-te-banner-frame {
    display: none !important;
}
```

2. **JavaScript** (كل 500ms):
```javascript
setInterval(hideGoogleTranslateBar, 500);
```

3. **ضبط body**:
```javascript
document.body.style.top = '0 !important';
```

## 🌍 اللغات المدعومة

- 🇸🇦 Arabic (ar)
- 🇺🇸 English (en)
- 🇵🇰 Urdu (ur)
- 🇷🇺 Russian (ru)
- 🇺🇿 Uzbek (uz)
- 🇯🇵 Japanese (ja)
- 🇮🇩 Indonesian (id)
- 🇵🇭 Filipino (fil)
- 🇳🇬 Hausa (ha)
- 🇸🇬 Singapore Chinese (zh-CN)
- 🇲🇾 Malay (ms)
- 🇨🇳 Mandarin Chinese (zh-CN)
- 🇲🇳 Mongolian (mn)
- 🇫🇷 French (fr)
- 🇹🇷 Turkish (tr)
- 🇮🇷 Persian (fa)

## 📝 إضافة رسالة ثابتة جديدة

### الخطوة 1: إضافة الرسالة في جميع اللغات

في `google-translate-controller.js`:

```javascript
const systemMessages = {
    ar: {
        // ... الرسائل الحالية
        myNewMessage: "رسالتي الجديدة"
    },
    en: {
        // ... الرسائل الحالية
        myNewMessage: "My New Message"
    }
    // ... باقي اللغات
};
```

### الخطوة 2: استخدام الرسالة في الكود

في `app.js` أو أي ملف آخر:

```javascript
const message = window.getSystemMessage('myNewMessage', window.currentGoogleLanguage);
element.textContent = message;
```

## 🔄 كيفية Google Translate يترجم المنتجات والفئات

Google Translate يترجم تلقائياً:
- **أسماء المنتجات**: من أي لغة إلى اللغة المختارة
- **أسماء الفئات**: من أي لغة إلى اللغة المختارة
- **محتوى الصفحة**: تلقائياً عند اختيار لغة جديدة

### مثال:
```
المنتج الأصلي: "شاورما الدجاج"
  ↓ (عند اختيار English)
المنتج المترجم: "Chicken Shawarma"
```

## ⚙️ المتغيرات العامة

| المتغير | الوصف |
|---------|-------|
| `currentGoogleLanguage` | اللغة المختارة الحالية |
| `systemMessages` | قاموس الرسائل الثابتة |
| `languageCodeMap` | خريطة الترجمة بين أكوادنا وأكواد Google |

## 🎯 الدوال الرئيسية

### `selectGoogleLanguage(lang)`
اختيار لغة في Google Translate بشكل برمجي

### `updateSystemMessages(lang)`
تحديث الرسائل الثابتة عند تغيير اللغة

### `hideGoogleTranslateBar()`
إخفاء شريط جوجل العلوي

### `getSystemMessage(key, lang)`
الحصول على رسالة ثابتة

### `updateUIMessages(lang)`
تحديث عناصر الواجهة بالرسائل الجديدة

## 📊 مقارنة مع النظام القديم

| الميزة | القديم | الجديد |
|--------|-------|--------|
| مصدر الترجمات | يدوي مشفر | Google Translate |
| رسائل النظام | يدوي | يدوي + Google Translate |
| الاعتمادية | عالية (يدوي) | عالية (Google) |
| سهولة التحديث | متوسطة | عالية |
| التكلفة | مجاني | مجاني (Web Element) |

## 🚀 الفوائد

✅ **ترجمات احترافية**: Google يوفر ترجمات عالية الجودة  
✅ **تحديثات تلقائية**: لا تحتاج تحديث يدوي للترجمات  
✅ **دعم 109 لغة**: يمكن إضافة لغات جديدة بسهولة  
✅ **بدون API مدفوع**: استخدام Web Element المجاني  
✅ **واجهة مخصصة**: تحكم كامل على الواجهة  

## ⚠️ ملاحظات مهمة

1. **الإنترنت مطلوب**: نظام Google Translate يحتاج اتصال إنترنت
2. **قد تحتاج وقتاً**: الترجمة قد تأخذ 1-2 ثانية عند اختيار لغة جديدة
3. **الترجمات غير مثالية**: قد تحتاج تصحيح يدوي لكلمات معينة

## 🔧 استكشاف الأخطاء

### المشكلة: لا تتغير اللغة
**الحل**: تأكد من أن Google Translate Script محمل:
```html
<script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
```

### المشكلة: لا تختفي الواجهة الافتراضية لجوجل
**الحل**: تحقق من أن `hideGoogleTranslateBar()` تعمل:
```javascript
console.log(document.querySelector('.goog-te-combo'));
```

### المشكلة: الرسائل لم تتحدث
**الحل**: تأكد من استدعاء `window.updateSystemMessages(lang)`:
```javascript
window.updateSystemMessages('en');
```
